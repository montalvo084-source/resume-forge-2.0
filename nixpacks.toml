[phases.setup]
nixPkgs = [
  "python312",
  "pango",
  "cairo",
  "gdk-pixbuf",
  "libffi",
  "gobject-introspection",
  "harfbuzz",
  "fontconfig",
  "freetype",
]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn app:app --bind 0.0.0.0:$PORT"
